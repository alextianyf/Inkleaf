# 印页待办事项

以下事项待处理，按反馈顺序记录。

1. [ ] **修复搜索栏尺寸调整。** 当前无法上下拖动改变高度，左右拖动调整尺寸的行为也不自然。左右应围绕水平中心对称伸缩，上下应围绕垂直中心对称伸缩；调整宽度与高度不应互相干扰。
2. [ ] **放大设置中“排版”的预览。** 当前预览过小，难以看清文字和排版细节，需要提供更易阅读的预览空间或缩放方式。
3. [ ] **为排版设置添加明确的保存操作。** 添加“应用更改”或“保存更改”按钮，并在保存成功后给出清晰反馈，让填写署名或调整其他选项的结果更直观。
4. [ ] **重新命名作者设置，并强化前后对比。** “作者”含义不够准确，考虑改为“署名与版权信息”等名称，具体文案待定。让添加内容在模板预览中的位置和变化更容易被注意到。
5. [ ] **进一步提升 PDF 美观性。** 优化字体、留白、标题层级、表格和分页等排版细节，具体方案待讨论。
6. [ ] **优化搜索。** 继续改善搜索速度、匹配准确度与结果展示，具体问题和方案后续补充。
7. [ ] **增加独立的开发模式。** 开发版使用独立的设置、搜索索引和缓存目录，与正式安装版分别管理单实例；采用不同的默认快捷键，托盘和窗口明确标为 `Inkleaf Dev`，关闭开发版自动更新及开机启动。提供明确的开发启动命令，保留正常界面与透明效果，让两个版本可以同时运行，互不覆盖设置或抢占快捷键。正式版继续兼容现有用户数据，自动测试仍使用独立临时目录。
8. [x] **修复 Alex Tian Logo 的居中问题。** 根因是主题将未指定宽度的 `<table align="center">` 撑至整页宽度，导致表格居中没有可见效果。布局层现在让显式对齐但没有有效宽度属性的 HTML 表格按内容取宽，再应用整体对齐；保留字符画内部空格、有效宽度属性和文档 CSS，普通 Markdown 表格仍为整页宽度。三个主题的左中右对齐、宽度优先级、实际预览与导出一致性测试通过；已目视确认 `Waves-and-Optics.md` 首页 Logo 居中，第 16 页公式正常，原文件字节与修改时间不变。修复位于当前开发分支，已发布的 0.4.10 尚未更新。
9. [x] **修复多行公式以 LaTeX 源码显示的问题。** 根因是多行公式用两端独占一行的 `$` 包围，当前解析器要求块级公式使用 `$$`。新增转换前检测：仅对完整顶层普通段落中的明确数学内容，经 KaTeX 验证后在内存副本中补全分隔符；无法解析时保留原文并提示行号。代码、HTML、正常公式和价格文本保持原样。已验证 `Waves-and-Optics.md` 的 38 处修复及全部 48 页 PDF，原文件字节与修改时间不变；截图对应页面的公式排版已目视确认。补充单元测试、实际预览/导出与中英文提示测试。修复位于当前开发分支，已发布的 0.4.10 尚未更新。
