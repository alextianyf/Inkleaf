# 印页待办事项

未完成事项保留在上方，完成事项归档到底部。完成不等于已发布，发布状态另行标注。

## 待完成

- [ ] **继续打磨现有主题。** 保留 Modern、Classic、Minimal，优先根据实际讲义反馈改进现有排版。中文字体切片内嵌已本地完成；着重号可单独评估。后续改动运行质量与灰度检查。
- [ ] **优化搜索。** 继续改善搜索速度、匹配准确度与结果展示，具体问题和方案后续补充。
- [ ] **代码模块化重构与简化。** 按职责梳理模块边界，拆分过大的文件与函数，减少重复逻辑；使用直观命名和容易理解的写法，在保持现有功能与运行速度的前提下提升可读性和可维护性。
- [ ] **优化 PDF 转换速度。** 先测量解析、资源加载、排版及 PDF 生成各阶段耗时，再针对瓶颈优化；分别关注首次预览、修改设置后的重复预览与文件夹批量导出，在保持渲染质量和布局正确性的前提下缩短等待时间。

- [ ] **重新评估标题编号（Heading numbering）与智能编号（Smart numbering）。** 当前已下架，仅保留后续设想，尚无重新上线计划。若继续开发，应由所有主题共用转换规则，主题只负责外观；考虑保留原文、智能识别已有编号、统一重新编号等模式，避免重复编号和误删年份等标题内容。目录需要结合名称与链接结构识别，并允许手动排除文档标题、前言等内容。所有修正仅作用于内存中的转换副本，保留源文件和锚点链接；需检查目录文字、正文中的章节引用、混合编号、跳级标题及跨页行为，再决定范围和默认策略。

## Completed

### 2026-09-21 · 本地完成，尚未发布

- [x] **保留黑体，内置 Modern 中文字体。** 完成日期：2026-09-21。采用 Noto Sans SC，保留 Inter 英文与 JetBrains Mono 代码字体；支持中文标题、正文、表格、代码注释和署名。101 个离线 WOFF2 切片约 4.3 MiB，按文档字符选择并内嵌，不在转换时联网取字体。附字体授权、来源及校验值。Classic、Minimal 不变，宋体样张仅留作比较；打印页眉页脚及字体未覆盖的字符仍使用系统字体。65 项单元测试、真实 Electron 离线字体检查及 Modern 中英文各 15 页质量模板检查通过。

- [x] **让转换问题明确可见。** 完成日期：2026-09-21。继续限制项目外本地图片；单文件和文件夹预览出现图片、布局或未修复语法问题时，在 PDF 上方显示可关闭的提醒，点击“查看详情”展开原因及引用。区分越界、文件不存在、读取权限、格式不支持、超时、图片过大、解码失败及数量上限。自动纠正成功的语法保留为普通文档提示。预览与导出失败显示明显错误卡片；导出成功和内容警告分别呈现，不修改源文件。新增真实 Electron 图片提醒回归测试。

- [x] **修复课件共享 Logo 与 badge 无法渲染。** 完成日期：2026-09-21。图片原先被限制在当前 Markdown 文件夹中，导致多层课件引用项目根目录的 `assets/branding/` 时被误拦截。现在允许最近 Git 项目内的共享相对路径（兼容 worktree），项目外路径及链接绕出仍拦截；非 Git 文档保留原文件夹边界。不修改源课件。JavaScript-Study-Guide 中 63 份文档的 311 处页头引用全部成功读取；第 00 章实际生成 18 页 PDF，16 张图片解码成功，首页 Logo 居中、四个 badge 高 20px，源文件校验一致。60 项单元测试及三套主题 HTML 布局回归通过，补充项目共享路径、嵌套项目、worktree 和越界路径测试。

### 2026-09-18 · 已随 0.6.0 发布

- [x] **将 Modern 设为默认。** 完成日期：2026-09-18。新配置、无效主题偏好及恢复排版默认使用 Modern；已有 Classic 与 Minimal 偏好保留。三套主题均可选，Modern 代码卡片不会改变另外两套主题的代码渲染。

- [x] **重做 Modern 主题并定稿。** 完成日期：2026-09-18。面向讲义：标题统一为一个蓝色系，每级有独立形状（渐隐粗线、色块、细线、短竖标、小型大写标签）；正文 Inter、代码 JetBrains Mono，均内置；正文相对标题缩进，标题与正文间距分组；圆角代码卡片带语言标签，五行以上自动行号；图片细边框、图注居中；表格去竖线、等宽数字；中文不再使用伪斜体。新增 `> [!NOTE]` 等五种标注块（标签随界面语言）及对应单元测试。Classic 与 Minimal 原有排版与代码呈现保留，新增标注块由三套主题共同支持。

- [x] **建立 Markdown → PDF 质量检查。** 完成日期：2026-09-18。`tests/quality/` 提供覆盖当前支持内容类别的模板与讲义样本；`npm run check:quality` 通过真实应用导出三种主题 × 中英文 PDF，检查缺失资源、排版提示、越界文字与内容标记，并生成页面图片与彩色／灰度总览；`npm run check:grayscale` 生成灰度近似图并检查白底文字对比度基准；实际背景、图片和打印效果仍需人工检查。移除旧的 Modern 评审样本、脚本与已下架主题（Folio、Dark 等）的测试产物。

### 2026-09-17 · 预览与导出反馈已随 0.6.0 发布；主题探索留档

- [x] **调整 Modern 第二版，优先评审英文。** 完成日期：2026-09-17。根据反馈撤下墨蓝统一配色，恢复分层标题色、黄色引用框、红色行内代码及高对比表头，保留较大代码字号和间距调整。新增完整英文样本，生成原版／新版各 10 页 PDF 与并排对照；中文模板保留，暂不继续优化。历史方案，已由 2026-09-18 的定稿替代；Classic 不变。

- [x] **制作 Modern 第一版与完整对照模板。** 完成日期：2026-09-17。加入墨蓝配色、中文字体回退、完整标题层级、更大代码与独立高亮配色、轻量表格和引用；Classic 样式及输出保持不变。使用同一份完整示例导出前后 PDF，提供逐页并排网页，覆盖公式、代码、强调、列表、表格、图片、HTML、脚注及跨页。历史方案，已由 2026-09-18 的定稿替代。

- [x] **复制 Classic 为 Modern。** 完成日期：2026-09-17。新增独立的 `resources/themes/modern.css`，初始内容与 Classic 完全一致；排版设置提供现代（Modern）选项，支持保存、模板预览与 PDF 导出。Classic 及 Minimal 保留，默认主题仍为 Classic，已有用户设置不改变。

- [x] **让预览进度与导出结果更直观。** 完成日期：2026-09-17。预览显示读取、排版、生成 PDF、显示页面四步进度，最后一步按实际页数推进；文件预览、文件夹预览和设置模板共用反馈。单文件保存成功后在 PDF 预览右上方悬浮显示带文件名的绿色提示，不占额外布局空间、不推动文档，底部保存路径保留；支持关闭提示，取消或保存失败不显示成功。批量任务全部成功时也显示明确确认，部分失败仍保留原有失败状态与重试入口。中英文及浅色／深色外观均支持。

### 2026-09-09 · 已随 0.5.1 发布

- [x] **下架 Folio 与标题编号功能。** 完成日期：2026-09-09。0.5.1 仅保留 Classic（默认）和 Minimal，旧 Folio 配置回退到 Classic，其他设置保留；移除标题编号设置、自动编号与重新编号处理，保留原文中的标题及编号。相关概念移至上方待办。此调整已随 0.5.1 发布，0.5.0 的历史记录保持原样。

- [x] **搜索结果单击预览。** 完成日期：2026-09-09。单击进入文件预览或文件夹批量预览，保留 Enter 打开及 Esc 返回，取消双击专属入口；已验证点击未选中的结果会打开实际点击的文件，加载期间可以取消。

- [x] **保持多行标题完整。** 完成日期：2026-09-09。能放入单页的标题保持在同一页；“标题与后文同页”独立控制后文是否跟随，超出整页的极端长标题仍可分页。此基础分页修复保留，不依赖自动编号。

### 2026-09-09 · 已随 0.5.0 发布

完成日期记录为 **2026-09-09**：以下改动未单独记录首次完成时间，以本次核对确认完成的日期归档。

- [x] **增加 App 外观模式。** 完成日期：2026-09-09。在 General（通用设置）提供浅色、深色、跟随系统，默认跟随系统；修改立即生效并自动保存，重启后保留，恢复通用设置时回到跟随系统。覆盖搜索栏、预览周边、设置各分类和确认弹窗；PDF 文档主题及页面颜色独立，未加入主题色选项。已验证多窗口同步、系统外观实时变化、手动覆盖、中英双语、保存与重置，切换前后 PDF 预览像素保持一致。此改动已随 0.5.0 发布。

- [x] **将 Folio（开本）设为默认主题。** 完成日期：2026-09-09。接入 Claude 提供的主题，保留 Classic 与 Minimal；新配置及恢复默认使用 Folio，已有用户保存的主题选择不覆盖。主题选择器、预览默认值及说明文档同步更新，此改动已随 0.5.0 发布。

- [x] **修复搜索栏尺寸调整。** 左右围绕水平中心、上下围绕垂直中心对称伸缩，宽高互不干扰；记住手动尺寸，重启后恢复，超出屏幕范围时限制尺寸。通用设置可恢复默认宽高。已通过边缘/角落、缩放取整、搜索结果变化与重启回归测试。

- [x] **放大设置中“排版”的预览。** 扩大排版窗口与模板区域，支持适合宽度、100%、125%、150% 缩放；小窗口中上下排列设置与预览，可滚动查看。普通设置不显示预览。

- [x] **为排版设置添加明确的保存操作。** 排版修改保留为草稿，模板实时更新；点击“保存排版”才生效，显示未保存/已保存反馈，支持还原。关闭或切换分类时提供保存、放弃和继续编辑；保存失败保留草稿、不应用失败的修改，也不会离开排版。普通设置仍自动保存。

- [x] **重新命名作者设置，并清楚展示更改位置。** © 左侧文字可自定义，留空使用文档标题；右侧填写版权所有者。只改变文档末尾的版权行，正文标题保持原样。“查看更改”跳到实际署名位置并柔和高亮两次，支持中英文和减少动态效果偏好。

- [x] **增加独立的开发模式。** `npm.cmd run dev` 启动 Inkleaf Dev，默认快捷键 Ctrl + Alt + Shift + Space；正式版默认仍为 Ctrl + Shift + Space。开发版使用 `%APPDATA%\Inkleaf-Dev`，正式版使用 `%APPDATA%\Inkleaf`，各自保存设置、索引、缓存和日志，分别管理单实例。开发版禁用更新与开机启动，保留正常界面和透明效果。新正式版首次启动会复制旧 Aldus 设置，保留原目录并重建索引，不覆盖已有 Inkleaf 设置。已通过两版同时运行、同版重复启动、设置隔离、双语标识、重置与重启恢复测试；迁移覆盖并发、不覆盖、损坏源文件等单元测试。此改动已随 0.5.0 发布。

- [x] **PDF 排版第一轮改进。** 已加入离线代码语法高亮，仅保留 Classic / Minimal；修复标题间距被相邻外边距盖过的问题，普通示例文档补齐 H1–H6，代码前用 H3 标明语言；六项分页规则提供真实 PDF 开关对比并定位页边界；“不强制分页”时开启规则会提示先在左侧选择标题级别；页眉、页脚支持查看更改，页码与署名位置变化自动定位高亮。字体、表格和整体留白仍在上方待办中。

### 2026-09-08 · 已随 0.4.11 发布

完成日期：**2026-09-08**，依据提交 `965250d`（公式渲染与表格对齐修复）。

- [x] **修复 Alex Tian Logo 的居中问题。** 根因是主题将未指定宽度的 `<table align="center">` 撑至整页宽度，导致表格居中没有可见效果。布局层现在让显式对齐但没有有效宽度属性的 HTML 表格按内容取宽，再应用整体对齐；保留字符画内部空格、有效宽度属性和文档 CSS，普通 Markdown 表格仍为整页宽度。当时三个主题的左中右对齐、宽度优先级、实际预览与导出一致性测试通过；已目视确认 `Waves-and-Optics.md` 首页 Logo 居中，第 16 页公式正常，原文件字节与修改时间不变。已随 0.4.11 发布。

- [x] **修复多行公式以 LaTeX 源码显示的问题。** 根因是多行公式用两端独占一行的 `$` 包围，当前解析器要求块级公式使用 `$$`。新增转换前检测：仅对完整顶层普通段落中的明确数学内容，经 KaTeX 验证后在内存副本中补全分隔符；无法解析时保留原文并提示行号。代码、HTML、正常公式和价格文本保持原样。已验证 `Waves-and-Optics.md` 的 38 处修复及全部 48 页 PDF，原文件字节与修改时间不变；截图对应页面的公式排版已目视确认。补充单元测试、实际预览/导出与中英文提示测试。已随 0.4.11 发布。
